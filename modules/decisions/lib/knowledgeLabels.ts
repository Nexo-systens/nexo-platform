/**
 * Rótulos de `KnowledgeCategory` (Mission 141, D-073) — extraídos de
 * `KnowledgeFormationPanel.tsx` (Mission 178) para reuso por
 * `modules/timeline/` sem duplicar o dicionário. Sempre evidenciais,
 * nunca causais — mesma disciplina de origem.
 */
export const KNOWLEDGE_CATEGORY_LABELS: Record<string, string> = {
  historical_pattern: "Padrão histórico (julgamento humano)",
  recurring_observation: "Observação financeira recorrente",
  accumulated_learning: "Aprendizado acumulado",
};
